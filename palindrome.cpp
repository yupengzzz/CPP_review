/*
**Yupeng Zhao
**2018-10-14
**The palindrome function takes in a string of characters
and returns the reverse of the string truncated at the end.
The last letter of the string is not repeated.
Example: "abc" -> "abcba" not "abccba"
*/

#include<iostream>
using namespace std;

char[] palindrome(char[] s);

int main()
{
  return 0;
}
